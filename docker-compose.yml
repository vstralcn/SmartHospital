services:
  backend:
    build: ./backend
    env_file:
      - .env
    expose:
      - "8000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "0.0.0.0:9080:80"
    depends_on:
      - backend
    restart: unless-stopped
